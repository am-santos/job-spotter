project_id = "your-gcp-project-id"
region     = "europe-west1"
env        = "prod"
# db_password is NOT here because it is a secret.
# Pass it via environment variable: export TF_VAR_db_password="secret_password"
